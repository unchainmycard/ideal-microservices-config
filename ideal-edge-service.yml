zuul:
  ignoredServices: '*'
  host:
    connect-timeout-millis: 20000
    socket-timeout-millis: 20000

  routes:
    ideal-test-service:
      path: /ideal-test-service/**
      serviceId: ideal-test-service
